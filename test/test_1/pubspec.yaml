name: test_1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0-99.1.beta <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  assets:
    - assets/images/
    - assets/icons/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
